import {
  color,
  space,
  layout,
  flexbox,
  border,
  shadow,
  typography,
} from 'styled-system';
import styled from '@emotion/styled';

export const Box = styled('div')(
  color,
  space,
  layout,
  flexbox,
  border,
  shadow,
  typography
);
